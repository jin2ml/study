# 第 4 月学习测验：Socket、TCP/UDP 与简单协议

## 理论题
1. TCP 和 UDP 的核心区别是什么？
2. 什么是粘包/拆包？
3. 为什么协议里常见长度字段？
4. `struct` 模块在二进制处理中有什么作用？

## 实操题
1. 写一个 TCP 客户端，连接本地服务端并发送消息。
2. 写一个 TCP 服务端，接收消息后回显。
3. 写一个 UDP 小程序，实现消息发送与接收。
4. 自定义“长度 + 类型 + 数据”报文并完成打包解包。

## 通过标准
- 能完成基础 TCP/UDP 通信实验
- 能读懂简单协议字段
- 能把字节流还原成结构化数据
